{
    "mcpServers": {
        "delivery-workflow": {
            "command": "bash",
            "args": [
                "-lc",
                "root=\"${CODEX_PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-$(pwd)}}\"; exec \"$root/scripts/deliveryflow-mcp\""
            ]
        }
    }
}
