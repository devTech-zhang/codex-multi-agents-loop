{
    "mcpServers": {
        "codex-delivery-workflow": {
            "command": "bash",
            "args": [
                "-lc",
                "root=\"$CODEX_PLUGIN_ROOT\"; if [ -z \"$root\" ]; then root=\"$(pwd)\"; fi; exec \"$root/scripts/codex-deliveryflow-mcp\""
            ]
        }
    }
}
