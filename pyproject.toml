[project]
name = "codex-multi-agents-loop"
version = "0.0.1"
description = "面向 Codex 的项目级多 Agent Loop 插件"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
codex-multi-agents-loop = "agents_workflow.cli:main"

[tool.setuptools.package-data]
agents_workflow = []
