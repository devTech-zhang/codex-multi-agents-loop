[project]
name = "codex-delivery-workflow"
version = "0.1.11"
description = "面向 Codex 的轻量交付工作流插件"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
codex-deliveryflow = "delivery_workflow.cli:main"

[tool.setuptools.package-data]
delivery_workflow = []
