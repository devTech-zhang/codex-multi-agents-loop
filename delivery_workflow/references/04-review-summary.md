# 评审意见汇总与最终 PRD

产品经理 Agent 根据 PRD v1 和评审报告生成最终 PRD。

如果输入中存在 `development-doc-confirmation_gate` 且确认结果为拒绝，必须把用户要求修改的理由作为本次修订的主约束，在保留合理内容的基础上修改最终 PRD；不要回退到需求录入或重新发起人工确认。

必须输出完整 Markdown，至少包含：

- 版本变化表格
- 各 Agent 评审意见汇总
- 相比 v1 变更点
- 已采纳意见
- 未采纳意见及原因
- 最终完整 PRD 内容

最终 PRD 需要能直接进入设计和技术方案阶段。不得只输出摘要。
